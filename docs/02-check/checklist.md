# 02-check 一致性修复 Checklist

## 总览摘要

共检查了 12 个模块，发现 58 处不一致，已修复 57 处。

- Pending：0 处
- Done：57 处
- Blocked：1 处
- 说明：除 `EIN-001` 因 `/design-docs/02-系统架构.md` 内部对 product/inventory 依赖方向存在冲突仍需先消歧外，其余条目均已按 `/docs/02-check/resolution.md` 完成修复。

## 明细

| 序号 | 所属模块 | 设计要求定位 | 不一致描述 | 修复方案概要 | 状态 | 备注 |
|---:|---|---|---|---|---|---|
| 1 | ecommerce-product | `design-docs/02-系统架构.md` §2、§3、§4 | 商品详情库存摘要由 `StockInfoFetcher` 本地硬编码返回，未通过 inventory 提供的 `InventoryQueryService` 查询。 | `ProductDetailService` 改为注入并调用 inventory 的 `InventoryQueryService#getStockSummary(Long skuId)`；移除或停用硬编码 `StockInfoFetcher`；联动修正 inventory DTO 契约。 | Done | 对应 `EP-001`；已在 `code/ecommerce-product/src/main/java/com/ecommerce/product/service/ProductDetailService.java:41,49,91` 停用 `StockInfoFetcher` 并改由库存查询契约取库存摘要；`code/ecommerce-app/src/main/java/com/ecommerce/app/InventoryQueryBridgeConfig.java:13-25` 在 app-bootstrap 层桥接 product 侧端口到 inventory 实现，避免因 `code/ecommerce-inventory/pom.xml` 已依赖 product 而新增 Maven 循环；`code/ecommerce-product/src/test/java/com/ecommerce/product/service/ProductDetailServiceTest.java:56,104,155` 同步测试。 |
| 2 | ecommerce-product | `design-docs/02-系统架构.md` §7 | 商品详情未按 `product:detail:{skuId}`、TTL 10 分钟建立缓存。 | 为 `ProductDetailService#getProductDetail(Long skuId)` 增加商品详情缓存，并在 SKU/SPU 变更、上下架时清理或更新缓存。 | Done | 对应 `EP-002`；已新增 `code/ecommerce-product/src/main/java/com/ecommerce/product/service/ProductDetailCacheService.java:19-36`，使用 `product:detail:{skuId}` 与 10 分钟 TTL；`ProductDetailService.java:64-70` 读写详情缓存；`code/ecommerce-product/src/main/java/com/ecommerce/product/service/SkuService.java:73-75,88-90,103-105` 在 SKU 创建、上架、下架后驱逐详情缓存；`code/ecommerce-product/pom.xml:25-28` 添加 Caffeine。 |
| 3 | ecommerce-product | `design-docs/02-系统架构.md` §3；`README.md` §6 | 管理创建 SPU/SKU 接口 Response 直接暴露 JPA Entity。 | 新增或复用 SPU/SKU 响应 DTO，`AdminProductController#createSpu/createSku` 返回 DTO，并保持 API 冻结契约不变。 | Done | 对应 `EP-003`；已新增 `code/ecommerce-product/src/main/java/com/ecommerce/product/dto/SpuResponse.java:12-29`、`code/ecommerce-product/src/main/java/com/ecommerce/product/dto/SkuResponse.java:11-34`；`code/ecommerce-product/src/main/java/com/ecommerce/product/controller/AdminProductController.java:46-59` 将 createSpu/createSku 改为返回 DTO，URL、Method、201 状态码未改。 |
| 4 | ecommerce-product | `README.md` §6.2、§7.2 | 公开商品详情 API 未对非 `ON_SHELF` SKU 返回 `PRODUCT_NOT_FOR_SALE`。 | 在 `ProductDetailService#getProductDetail` 查询 SKU 后校验状态，非上架时抛 `BusinessException("PRODUCT_NOT_FOR_SALE", ...)`。 | Done | 对应 `EP-004`；已在 `code/ecommerce-product/src/main/java/com/ecommerce/product/service/ProductDetailService.java:73-79` 查询 SKU 后校验 `SkuStatus.ON_SHELF`，非上架抛 `BusinessException("PRODUCT_NOT_FOR_SALE", ...)`；`code/ecommerce-product/src/test/java/com/ecommerce/product/service/ProductDetailServiceTest.java:161-173` 覆盖非上架异常。 |
| 5 | ecommerce-common | `design-docs/02-系统架构.md` §5 | common notification 缺少对注册、下单、支付、退款等核心事件的通知监听适配。 | 在 common notification 新增事件监听适配类，监听相关领域事件并调用 `LocalNotificationService`，异常只记录不外抛；避免 common 反向强依赖业务模块。 | Done | 对应 `EC-COMMON-001`；已新增 `code/ecommerce-common/src/main/java/com/ecommerce/common/notification/CoreNotificationEventListener.java:20`、`:37`，通过监听 common 可见 `AbstractDomainEvent` 并按事件 simpleName/反射字段适配通知，避免 common 反向强依赖 user/order/payment 等业务模块；监听器异常兜底并写入 common 失败记录。 |
| 6 | ecommerce-common | `design-docs/02-系统架构.md` §5、§6 | `notification-send-failure` 故障注入在 `LocalNotificationServiceImpl.send()` 内部 try/catch 前直接抛出，可能影响主流程。 | 将故障注入检查纳入失败处理范围，或记录失败后返回；通知监听器也需兜底捕获异常。 | Done | 对应 `EC-COMMON-002`；已调整 `code/ecommerce-common/src/main/java/com/ecommerce/common/notification/LocalNotificationServiceImpl.java:74` 起的 send 内部 try/catch，将 `notification-send-failure` 纳入失败处理范围；联动 `CoreNotificationEventListener.java:43` 对监听器调用再做异常兜底。 |
| 7 | ecommerce-common | `design-docs/02-系统架构.md` §8；`README.md` §6.8 | common 拥有测试支撑能力但自身未暴露对应 REST 管理接口，当前访问层在 app。 | 若验收要求 common 承担访问层，则在 common 新增管理 Controller；若保留 app 入口，则确认扫描和契约一致，避免重复路由。 | Done | 对应 `EC-COMMON-003`；已核查 `code/ecommerce-app/src/main/java/com/ecommerce/app/ShopHubApplication.java:16` 扫描 `com.ecommerce`，app 统一 REST 入口覆盖 README §6.8：`SystemAdminController.java:23`/`:35`/`:44`/`:81`、`FaultInjectionAdminController.java:18`/`:29`、`EventFailureAdminController.java:25`、`NotificationAdminController.java:19`；与 common 能力联动但不新增 common Controller，避免重复路由冲突。 |
| 8 | ecommerce-user | `design-docs/02-系统架构.md` §1、§3、§5 | 用户注册在事务内同步发送欢迎通知，未发布 `UserRegisteredEvent`，通知失败可能回滚注册。 | 新增 `UserRegisteredEvent`，注册服务改为发布事件；由监听器发送通知并捕获异常记录日志/失败记录。 | Done | 对应 `EU-001`；已新增 `code/ecommerce-user/src/main/java/com/ecommerce/user/event/UserRegisteredEvent.java:9,15`，字段包含 `userId`/`email`/`nickname`；`code/ecommerce-user/src/main/java/com/ecommerce/user/service/UserRegisterService.java:28,32,59` 移除 `LocalNotificationService` 直接依赖并发布注册事件；与 common notification 事件监听联动：`code/ecommerce-common/src/main/java/com/ecommerce/common/notification/CoreNotificationEventListener.java:58,67` 识别 `UserRegisteredEvent` 并兜底发送欢迎通知。 |
| 9 | ecommerce-user | `design-docs/02-系统架构.md` §7 | 用户权限缓存 `user:roles:{userId}`、TTL 30 分钟未实现。 | 新增角色缓存配置/管理组件；登录和鉴权读写该缓存；冻结/解冻或角色变更时失效/刷新。 | Done | 对应 `EU-002`；已在 `code/ecommerce-user/pom.xml:26-27` 添加 Caffeine；新增 `code/ecommerce-user/src/main/java/com/ecommerce/user/cache/UserRoleCacheManager.java:19,24-25,29,34,42,47`，实现 `user:roles:{userId}` 与 30 分钟 TTL；`code/ecommerce-user/src/main/java/com/ecommerce/user/service/UserAuthService.java:77,130,143` 登录写入、冻结刷新为空角色、解冻刷新当前角色；`code/ecommerce-user/src/main/java/com/ecommerce/user/security/JwtAuthFilter.java:65-68` 鉴权优先读缓存、未命中回填；`code/ecommerce-user/src/main/java/com/ecommerce/user/config/SecurityConfig.java:37-38,56` 与 `code/ecommerce-app/src/main/java/com/ecommerce/app/SecurityConfig.java:51-52,71` 接入角色缓存过滤器。 |
| 10 | ecommerce-inventory | `design-docs/02-系统架构.md` §2、§3、§4 | inventory 依赖 product 符合 §4 `ProductQueryService` 使用方，但与 §2 依赖图 product → inventory 冲突。 | 需先消歧设计基准；确定后再按最终方向调整 Maven/代码依赖，避免循环或反向依赖。 | Blocked | 对应 `EIN-001`；设计文档内部约束冲突。 |
| 11 | ecommerce-inventory | `design-docs/02-系统架构.md` §1、§3 | inventory 模块定义 product 表 Entity 和 `ProductRepository`，违反模块数据边界。 | 删除 inventory 内 `Product` Entity 与 `ProductRepository`；商品信息通过跨模块查询契约或上层组合获取。 | Done | 对应 `EIN-002`；已删除 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/entity/Product.java:1` 与 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/repository/ProductRepository.java:1`，无残留引用。 |
| 12 | ecommerce-inventory | `design-docs/02-系统架构.md` §3、§4 | `InventoryQueryService#getStockSummary` 返回 product 包 `StockSummaryDto`，契约泄露 product 类型。 | 返回类型改为 `com.ecommerce.inventory.query.StockSummaryDto`，同步调整实现和 product/cart/order 使用方。 | Done | 对应 `EIN-003`；已修改 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/query/InventoryQueryService.java:19`、`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/service/InventoryService.java:59` 返回 inventory DTO，并最小联动 `code/ecommerce-cart/src/main/java/com/ecommerce/cart/service/CartValidationService.java:5`、`code/ecommerce-cart/src/test/java/com/ecommerce/cart/service/CartValidationServiceTest.java:5`；后续仍需与 product/cart/order 使用方条目联动，未在本项消歧 EIN-001 的 product↔inventory 设计冲突。 |
| 13 | ecommerce-inventory | `design-docs/02-系统架构.md` §7 | 库存摘要缓存 `inventory:summary:{skuId}`、TTL 30 秒未实现。 | 为库存摘要查询增加缓存；在入库、出库、调整、预占、释放、扣减后驱逐或更新缓存。 | Done | 对应 `EIN-004`；已新增 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/cache/InventorySummaryCache.java:12` 使用 `inventory:summary:{skuId}`、30 秒 TTL；查询接入 `InventoryService.java:59`/`:91`，入库/出库失效 `InventoryService.java:151`/`:180`，调整失效 `StockAdjustmentService.java:48`，预占/释放/扣减失效 `InventoryReservationServiceImpl.java:56`/`:92`/`:114`。 |
| 14 | ecommerce-inventory | `design-docs/02-系统架构.md` §8；`README.md` §6.3 | 库存管理接口未在模块内体现 ADMIN 角色约束。 | 在 `AdminInventoryController` 类或方法添加 `@PreAuthorize("hasRole('ADMIN')")`，并确保方法级安全生效或全局规则覆盖。 | Done | 对应 `EIN-005`；已在 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/controller/AdminInventoryController.java:28` 添加类级 `@PreAuthorize("hasRole('ADMIN')")`，并在 `code/ecommerce-inventory/pom.xml:27` 增加 security 注解编译依赖。 |
| 15 | ecommerce-inventory | `README.md` §6、§6.3 | `POST /api/v1/admin/inventory/outbound` 使用 query param 承载业务字段，未使用请求体 DTO。 | 新增 `OutboundRequest` DTO，Controller 改为 `@Valid @RequestBody`，字段名和类型保持冻结契约。 | Done | 对应 `EIN-006`；已新增 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/dto/OutboundRequest.java:7`，字段 `warehouseId`/`skuId`/`quantity`/`orderId`；`AdminInventoryController.java:59` 改为 `@Valid @RequestBody OutboundRequest`，URL/Method/201 不变。 |
| 16 | ecommerce-inventory | `README.md` §6、§6.3 | `POST /api/v1/admin/inventory/adjustments` 使用 query param 承载业务字段，未使用请求体 DTO。 | 新增 `StockAdjustmentRequest`/`AdjustmentRequest` DTO，Controller 改为 `@Valid @RequestBody`。 | Done | 对应 `EIN-007`；已新增 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/dto/StockAdjustmentRequest.java:7`，字段 `warehouseId`/`skuId`/`afterQty`/`reason`；`AdminInventoryController.java:67` 改为 `@Valid @RequestBody StockAdjustmentRequest`，URL/Method/201 不变。 |
| 17 | ecommerce-review | `design-docs/02-系统架构.md` §2、§3、§5 | review POM 直接依赖 loyalty，与 `ReviewApprovedEvent` 由 review 发布、loyalty 监听的事件解耦方向不一致。 | 删除 `ecommerce-review` 对 `ecommerce-loyalty` 的 Maven 依赖；review 只发布事件。 | Done | 对应 `ER-001`；已在 `code/ecommerce-review/pom.xml:22-25` 删除 loyalty 依赖并改为 user 查询依赖，review 不再依赖 loyalty。 |
| 18 | ecommerce-review | `design-docs/02-系统架构.md` §4；`README.md` §7.2 | review 未使用 user `UserQueryService` 查询用户激活/冻结状态。 | 注入 `UserQueryService`，创建评价前校验 `isActive/isFrozen`，分别抛 `USER_NOT_ACTIVE`、`USER_FROZEN`。 | Done | 对应 `ER-002`；已在 `code/ecommerce-review/pom.xml:22-25` 增加 user 查询依赖；`code/ecommerce-review/src/main/java/com/ecommerce/review/service/ReviewService.java:16,42,48,71,109-114` 注入 `UserQueryService` 并按 `isActive/isFrozen` 抛 `USER_NOT_ACTIVE`/`USER_FROZEN`；测试同步 `ReviewServiceTest.java:16,68,95-96,178-196`。 |
| 19 | ecommerce-review | `design-docs/02-系统架构.md` §5 | 创建评价时状态仍为 `PENDING_REVIEW` 却立即发布 `ReviewApprovedEvent`。 | 移除创建评价时的事件发布，仅在审核通过并保存 `APPROVED` 后发布。 | Done | 对应 `ER-003`；已从 `code/ecommerce-review/src/main/java/com/ecommerce/review/service/ReviewService.java:101-107` 移除创建评价时发布事件；仅保留 `ReviewModerationService.java:63` 在审核通过后发布；`ReviewServiceTest.java:165-174` 验证创建时不发布。 |
| 20 | ecommerce-review | `design-docs/02-系统架构.md` §5 | review 模块内部监听 `ReviewApprovedEvent` 并模拟发放评价积分，职责应属于 loyalty。 | 删除或停用 review 内 `ReviewApprovedEventListener`，积分发放只由 loyalty 监听器处理。 | Done | 对应 `ER-004`；已在 `code/ecommerce-review/src/main/java/com/ecommerce/review/service/ReviewApprovedEventListener.java:9-20` 停用 Spring 注册，移除 `@Component`/`@EventListener`，积分监听由 loyalty 处理。 |
| 21 | ecommerce-review | `design-docs/02-系统架构.md` §5 | `ReviewApprovedEvent` 在 review 与 loyalty 中重复定义，发布类型与监听类型不匹配。 | 将事件统一为单一公共契约，review 发布同一类型，loyalty 监听同一类型并删除重复事件类。 | Done | 对应 `ER-005`；联动 `ELT-003`。已新增统一公共契约 `code/ecommerce-common/src/main/java/com/ecommerce/common/event/ReviewApprovedEvent.java:7`；review 发布改用 `ReviewModerationService.java:4,63`；loyalty 监听改用 `code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ReviewApprovedEventListener.java:4,27`；删除 review/loyalty 本地重复事件类并同步测试引用 `ReviewApprovedEventListenerTest.java:4`。 |
| 22 | ecommerce-order | `design-docs/02-系统架构.md` §3、§4 | 订单创建积分抵扣使用 `LoyaltyQueryService` 估算，未通过 `LoyaltyCommandService` 执行命令。 | 注入并调用 `LoyaltyCommandService` 完成积分冻结/消费/解冻或抵扣；估算仅保留为展示/校验。 | Done | 对应 `EO-001`；已在 `code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderService.java:260-269` 改为创建订单事务内 `freezePoints`，`OrderPaymentEventHandler.java:123-126` 支付成功后 `consumeFrozenPoints`，`OrderCancelService.java:232-238` 取消订单时 `unfreezePoints`。 |
| 23 | ecommerce-order | `design-docs/02-系统架构.md` §4、§5、§6 | `OrderPaymentStatusUpdater.markAsPaid` 仅更新订单支付状态，未扣减库存、未发布 `OrderPaidEvent`。 | 委托统一支付成功处理服务，或在同事务内完成订单状态更新、库存扣减和事件发布。 | Done | 对应 `EO-002`；已在 `code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderQueryServiceImpl.java:123-129` 将 `markAsPaid` 委托给统一支付成功处理；`OrderPaymentEventHandler.java:71-131` 在同事务内完成订单置为 `PAID`、库存扣减并发布 `OrderPaidEvent`。 |
| 24 | ecommerce-order | `design-docs/02-系统架构.md` §6 | 支付成功处理吞掉库存扣减异常，仍可能发布 `OrderPaidEvent`。 | 库存扣减失败必须抛出并回滚支付确认；只有物流/积分/通知后置失败可补偿且不阻塞支付。 | Done | 对应 `EO-003`；已在 `code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderPaymentEventHandler.java:118-131` 去除库存扣减异常吞掉逻辑，`deductAfterPayment(orderId)` 失败将直接回滚支付确认事务，事件仅在成功后发布。 |
| 25 | ecommerce-order | `design-docs/02-系统架构.md` §6 | 批量订单导入被类级事务包住整批循环，未按单条事务处理。 | 移除整批事务或通过 `REQUIRES_NEW` 单条事务方法提交每单，失败不回滚已成功订单。 | Done | 对应 `EO-004`；已在 `code/ecommerce-order/src/main/java/com/ecommerce/order/service/BatchOrderService.java:25-32,49-70` 去除整批事务，改为 `TransactionTemplate` 对每单独立提交，`continueOnError=false` 仅停止后续处理。 |
| 26 | ecommerce-order | `design-docs/02-系统架构.md` §6 | 创建订单只计算优惠并快照 couponIds，未写优惠使用记录。 | 在创建订单事务中调用 promotion 优惠使用命令，锁定/核销优惠券并记录订单、用户、金额。 | Done | 对应 `EO-005`；联动 `EPRO-003`。已在 `code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderService.java:267-269` 调用 `promotionUsageCommandService.markCouponsUsed(...)`，并在 `OrderService.java:94-96,112-130` 注入 promotion 优惠使用命令接口。 |
| 27 | ecommerce-order | `README.md` §7.1、§7.2 | 订单模块使用 `BATCH_ORDER_FAILED`、`ORDER_NOT_PAYABLE`、`ORDER_INVALID_STATUS` 等未冻结错误码。 | 映射到 `RESOURCE_NOT_FOUND`、`ORDER_STATUS_CONFLICT`、`PRODUCT_NOT_FOR_SALE`、`VALIDATION_FAILED`、`CONFLICT` 等冻结错误码。 | Done | 对应 `EO-006`；已在 `code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderPreconditionChecker.java:34,46`、`OrderValidator.java:37,47`、`OrderCancelService.java:72,240-241`、`OrderQueryServiceImpl.java:72`、`code/ecommerce-order/src/main/java/com/ecommerce/order/integration/ProductIntegrationService.java:67` 收敛到冻结错误码，并在 `BatchOrderService.java:67-69` 去除 `BATCH_ORDER_FAILED` 抛出路径。 |
| 28 | ecommerce-promotion | `design-docs/02-系统架构.md` §3、§4 | `PromotionCalculationService` 当前是具体 `@Service` 类，跨模块抽象实际在 common port，接口形态不符合设计。 | 将 `PromotionCalculationService` 改为 promotion 提供的本地接口，现有逻辑迁移到实现类，order/cart 通过 DTO 调用。 | Done | 对应 `EPRO-001`；已将 `code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/service/PromotionCalculationService.java:9,14` 改为 promotion 本地接口；新增 `code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/service/PromotionCalculationServiceImpl.java:23,67` 承载原计算逻辑并兼容旧 common port；`/api/v1/promotions/calculate` 未变。 |
| 29 | ecommerce-promotion | `design-docs/02-系统架构.md` §1、§2、§4 | cart 未依赖 promotion，而是通过 common `PromotionDiscountCalculator` 计算优惠。 | cart 增加对 promotion 接口依赖，`CartService` 注入 `PromotionCalculationService` 并做 DTO 转换。 | Done | 对应 `EPRO-002`；联动 `ECART-002` 同步完成。已在 `code/ecommerce-cart/pom.xml:29` 增加 promotion 依赖；`code/ecommerce-cart/src/main/java/com/ecommerce/cart/service/CartService.java:15,17,46,51,164,177,277,282` 改为注入 promotion `PromotionCalculationService` 并转换为 `PromotionCalculateRequest` DTO，停止直接依赖 common `PromotionDiscountCalculator`。 |
| 30 | ecommerce-promotion | `design-docs/02-系统架构.md` §6 | promotion 未提供订单创建事务所需的优惠使用记录写入能力。 | 新增 `CouponUsageService`/`PromotionUsageCommandService`，设置 `CouponStatus.USED`、`usedOrderId`、`usedAt` 并供 order 调用。 | Done | 对应 `EPRO-003`；新增 `code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/service/PromotionUsageCommandService.java:8` 本地命令接口，`code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/service/CouponUsageService.java:18,47-49` 校验用户券后写入 `CouponStatus.USED`、`usedOrderId`、`usedAt`；供 order/EO-005 后续调用。 |
| 31 | ecommerce-promotion | `design-docs/02-系统架构.md` §8；`README.md` §6.7 | 促销用户侧接口未体现 USER 角色约束。 | 在 `PromotionController` 用户侧方法或类上添加 `@PreAuthorize("hasRole('USER')")`，或确认全局鉴权覆盖。 | Done | 对应 `EPRO-004`；已在 `code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/controller/PromotionController.java:18,36` 显式添加 USER 角色约束，用户侧 URL/Method/成功状态码未变。 |
| 32 | ecommerce-promotion | `design-docs/02-系统架构.md` §8；`README.md` §6.7 | 促销管理端接口未体现 ADMIN 角色约束。 | 在 `AdminPromotionController` 添加 `@PreAuthorize("hasRole('ADMIN')")`，或确认 `/api/v1/admin/**` 全局规则覆盖。 | Done | 对应 `EPRO-005`；已在 `code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/controller/AdminPromotionController.java:14,26` 显式添加 ADMIN 角色约束，管理侧 URL/Method/成功状态码未变。 |
| 33 | ecommerce-promotion | `README.md` §7.1、§7.2 | promotion 使用 `COUPON_LIMIT_EXCEEDED`、`COUPON_EXHAUSTED`、`SECKILL_*` 等未冻结错误码。 | 收敛到 `COUPON_EXPIRED`、`RESOURCE_NOT_FOUND`、`CONFLICT`、`VALIDATION_FAILED` 等冻结错误码。 | Done | 对应 `EPRO-006`；已将未冻结错误码收敛到 README 第 7 章：`code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/service/CouponService.java:47,54` 使用 `CONFLICT`，`CouponValidator.java:34` 使用 `RESOURCE_NOT_FOUND`，`SeckillService.java:57,61,69` 使用 `COUPON_EXPIRED`/`CONFLICT`；同步测试断言。 |
| 34 | ecommerce-logistics | `design-docs/02-系统架构.md` §4 | logistics 未提供 `LogisticsCommandService`，事件监听器直接操作 Repository/Service/订单查询。 | 新增 `LogisticsCommandService` 及实现，封装按已支付订单创建发货单；监听器只依赖该接口。 | Done | 对应 `EL-001`；已新增 `code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/service/LogisticsCommandService.java:8`、`LogisticsCommandServiceImpl.java:19`，封装按已支付订单创建发货单；`code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/event/OrderPaidShipmentListener.java:21-24,28-31` 仅依赖命令接口。测试命令 `mvn -f code/pom.xml test -pl ecommerce-logistics -am` 已运行，失败于上游 `code/ecommerce-user/src/main/java/com/ecommerce/user/service/UserRegisterService.java:59` 既有测试空指针，非本项范围；`-DskipTests` 编译通过。 |
| 35 | ecommerce-logistics | `design-docs/02-系统架构.md` §5 | logistics 未监听 `PaymentSucceededEvent`，仅监听 `OrderPaidEvent`。 | 新增 `PaymentSucceededEvent` 监听器，复用 `LogisticsCommandService` 创建发货单，异常记录不回滚支付。 | Done | 对应 `EL-002`；已在 `code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/event/OrderPaidShipmentListener.java:4,35-37` 监听 `PaymentSucceededEvent`，并在 `:40-48` 捕获异常仅记录日志；`code/ecommerce-logistics/pom.xml:22-25` 增加 payment 事件类型依赖。测试命令 `mvn -f code/pom.xml test -pl ecommerce-logistics -am` 已运行，仍失败于上游 `ecommerce-user` 既有单测空指针；`-DskipTests` 编译通过。 |
| 36 | ecommerce-logistics | `design-docs/02-系统架构.md` §5 | 物流签收后未发布 `ShipmentDeliveredEvent`。 | 新增 `ShipmentDeliveredEvent`，在 `ShipmentService#updateStatus` 保存 `DELIVERED` 后发布，供 order/loyalty 监听。 | Done | 对应 `EL-003`；已新增 `code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/event/ShipmentDeliveredEvent.java:10-37`；`code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/service/ShipmentService.java:288-296` 在保存 DELIVERED 和轨迹后发布事件，供 order/loyalty 后续监听联动。测试命令 `mvn -f code/pom.xml test -pl ecommerce-logistics -am` 已运行，仍失败于上游 `ecommerce-user` 既有单测空指针；`-DskipTests` 编译通过。 |
| 37 | ecommerce-logistics | `design-docs/02-系统架构.md` §4、§5 | 签收状态同步使用设计未列出的 `OrderLogisticsStatusUpdater` 同步接口。 | 停止签收链路同步调用该接口，由 order 监听 `ShipmentDeliveredEvent` 更新签收状态。 | Done | 对应 `EL-004`；`code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/service/ShipmentService.java:288-299` 签收 DELIVERED 分支改为发布 `ShipmentDeliveredEvent`，不再同步调用 `OrderLogisticsStatusUpdater`；中间状态仍保留同步更新。测试命令 `mvn -f code/pom.xml test -pl ecommerce-logistics -am` 已运行，仍失败于上游 `ecommerce-user` 既有单测空指针；`-DskipTests` 编译通过。 |
| 38 | ecommerce-logistics | `design-docs/02-系统架构.md` §7 | 运费模板缓存 `logistics:freight:{templateId}`、TTL 30 分钟未实现。 | 为运费模板读取增加缓存，创建/更新/停用模板时清理对应缓存。 | Done | 对应 `EL-005`；已新增 `code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/service/FreightTemplateCache.java:16-38`，使用 `logistics:freight:{templateId}` 与 30 分钟 TTL；`FreightCalculator.java:29,72,99` 读取模板走缓存；`FreightTemplateService.java:56-57,95,128` 创建/更新/删除模板时清理缓存；`code/ecommerce-logistics/pom.xml:27-29` 添加 Caffeine。测试命令 `mvn -f code/pom.xml test -pl ecommerce-logistics -am` 已运行，仍失败于上游 `ecommerce-user` 既有单测空指针；`-DskipTests` 编译通过。 |
| 39 | ecommerce-logistics | `README.md` §6.7 | 物流管理发货流程路径变量使用 `{id}`，与冻结契约 `{shipmentId}` 不一致。 | 将三个 `@PostMapping` 路径变量和参数改为 `{shipmentId}`/`shipmentId`。 | Done | 对应 `EL-006`；已将 `code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/controller/AdminLogisticsController.java:45-68` 三个管理发货流程改为 `{shipmentId}` 与 `@PathVariable("shipmentId") Long shipmentId`，URL 层级、Method、200 成功状态码未变。测试命令 `mvn -f code/pom.xml test -pl ecommerce-logistics -am` 已运行，仍失败于上游 `ecommerce-user` 既有单测空指针；`-DskipTests` 编译通过。 |
| 40 | ecommerce-logistics | `README.md` §7.1 | 物流状态冲突抛 `IllegalStateException`，会映射为 `INTERNAL_ERROR`/500，而非 `CONFLICT`/409。 | 非法状态流转改抛 `ConflictException` 或 `BusinessException("CONFLICT", ...)`，补齐不可跳步校验。 | Done | 对应 `EL-007`；已在 `code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/service/ShipmentService.java:147-153,194-200,247-253` 对 PICKING、LABEL_PRINTED、OUTBOUND 不可跳步状态流转抛 `ConflictException`，辅助方法见 `:311-314`，统一映射 409/CONFLICT；同步测试 `code/ecommerce-logistics/src/test/java/com/ecommerce/logistics/service/ShipmentServiceTest.java:102-107,174-188,194-200`。测试命令 `mvn -f code/pom.xml test -pl ecommerce-logistics -am` 已运行，失败于上游 `ecommerce-user`/`ecommerce-promotion` 既有单测问题，非本项范围；`-DskipTests` 编译通过。 |
| 41 | ecommerce-loyalty | `design-docs/02-系统架构.md` §4 | 会员等级统计使用 loyalty 自定义 `AnnualConsumptionQueryService`，未通过 order `OrderQueryService`。 | 扩展 order `OrderQueryService` 提供年度已支付消费查询，loyalty 强依赖并调用该接口，删除静默降级。 | Done | 对应 `ELT-001`；与 ecommerce-order `OrderQueryService` 联动，`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/service/MemberLevelService.java:28-33,52-80` 强依赖并调用 `orderQueryService.getAnnualConsumption(userId)`，`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/service/MemberLevelServiceTest.java:33-40,47-123` 同步改测；order 年度消费查询位于 `code/ecommerce-order/src/main/java/com/ecommerce/order/query/OrderQueryService.java:60`、`code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderQueryServiceImpl.java:114-120`；已删除 `code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/query/AnnualConsumptionQueryService.java`。 |
| 42 | ecommerce-loyalty | `design-docs/02-系统架构.md` §5 | loyalty 重复定义并监听本模块 `OrderPaidEvent`，与 order 发布类型不一致。 | 统一 `OrderPaidEvent` 契约，loyalty 监听 order/common 的同一事件类型，删除本地重复事件。 | Done | 对应 `ELT-002`；与 order 唯一 `OrderPaidEvent` 联动，`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/OrderPaidEventListener.java:31-41,57-73` 改监听 `com.ecommerce.common.event.OrderPaidEvent` 并保留失败持久化，`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/event/OrderPaidEventListenerTest.java:32-43,57-108` 同步改为 common 事件；已删除 `code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/OrderPaidEvent.java`。 |
| 43 | ecommerce-loyalty | `design-docs/02-系统架构.md` §5 | loyalty 重复定义并监听本模块 `ReviewApprovedEvent`，与 review 发布类型不一致。 | 统一 `ReviewApprovedEvent` 契约，loyalty 监听 review/common 的同一事件类型，删除本地重复事件。 | Done | 对应 `ELT-003`；与 ecommerce-review/ER-005 联动。已删除 `code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ReviewApprovedEvent.java:1`，`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ReviewApprovedEventListener.java:4,27` 改监听 common 统一事件，测试引用同步 `code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/event/ReviewApprovedEventListenerTest.java:4`。 |
| 44 | ecommerce-loyalty | `design-docs/02-系统架构.md` §5 | loyalty 缺少 `PaymentSucceededEvent` 和 `ShipmentDeliveredEvent` 监听入口。 | 新增两个事件监听器；若缺少 `ShipmentDeliveredEvent`，先由 logistics/common 建立事件契约。 | Done | 对应 `ELT-004`；与 payment/logistics 事件联动，`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/PaymentSucceededEventListener.java:21-41`、`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ShipmentDeliveredEventListener.java:21-41` 新增监听入口并兜底异常不回滚主流程，分别对接 payment `PaymentSucceededEvent` 与 logistics `ShipmentDeliveredEvent` 的既有事件契约。 |
| 45 | ecommerce-loyalty | `design-docs/02-系统架构.md` §5、§6 | `OrderPaidEvent` 积分发放失败只记录日志，未持久化补偿任务。 | 在 listener 异常兜底中保存失败记录或补偿任务，异常仍不外抛。 | Done | 对应 `ELT-005`；`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/OrderPaidEventListener.java:30-37,55-77` 注入并复用 common `FailedEventRecordRepository`/`FailedEventRecord` 持久化失败记录，`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/event/OrderPaidEventListenerTest.java:35-43,91-109` 覆盖补偿记录落库断言。 |
| 46 | ecommerce-loyalty | `design-docs/02-系统架构.md` §8；`README.md` §6.7 | loyalty 管理接口只用注释说明 ADMIN，无可执行角色约束。 | 在 `AdminLoyaltyController` 或 `expirePoints()` 上添加 `@PreAuthorize("hasRole('ADMIN')")`。 | Done | 对应 `ELT-006`；`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/controller/AdminLoyaltyController.java:20-21` 已添加类级 `@PreAuthorize("hasRole('ADMIN')")`，`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/controller/AdminLoyaltyControllerTest.java:31-115` 保持管理接口测试通过。 |
| 47 | ecommerce-cart | `design-docs/02-系统架构.md` §3、§4 | cart 注入 product 包 `InventoryQueryService` 和 `StockSummaryDto`，而设计要求由 inventory 提供。 | `CartValidationService` 改为使用 inventory 包 `InventoryQueryService`/`StockSummaryDto`，同步测试 mock/import。 | Done | 对应 `ECART-001`；已改 `code/ecommerce-cart/src/main/java/com/ecommerce/cart/service/CartValidationService.java:5-6,26,63`、`code/ecommerce-cart/src/test/java/com/ecommerce/cart/service/CartValidationServiceTest.java:5-8`；与 `ecommerce-inventory/EIN-003` 联动，同步 `code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/query/InventoryQueryService.java:19`、`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/service/InventoryService.java:60`。 |
| 48 | ecommerce-cart | `design-docs/02-系统架构.md` §4 | 购物车价格预估使用 common `PromotionDiscountCalculator`，未使用 promotion `PromotionCalculationService`。 | `CartService` 改为注入 promotion `PromotionCalculationService`，转换购物车项和 couponIds DTO。 | Done | 对应 `ECART-002`；联动 `EPRO-001/002` 同步完成。已在 `code/ecommerce-cart/pom.xml:29` 增加 promotion 依赖；`code/ecommerce-cart/src/main/java/com/ecommerce/cart/service/CartService.java:46,51,164,177,277,282` 使用 promotion 计算接口和 DTO；`code/ecommerce-cart/src/test/java/com/ecommerce/cart/service/CartServiceTest.java:16,49` 同步 mock。 |
| 49 | ecommerce-cart | `design-docs/02-系统架构.md` §7 | 购物车缓存以裸 `Long userId` 为 key，未使用 `cart:{userId}`。 | 缓存 key 类型改为 String，统一生成 `cart:{userId}`，保存/读取/删除均使用该 key。 | Done | 对应 `ECART-003`；已改 `code/ecommerce-cart/src/main/java/com/ecommerce/cart/cache/CartCacheManager.java:17,19,30,47,58,63-64`、`code/ecommerce-cart/src/main/java/com/ecommerce/cart/config/CartCacheConfig.java:38,41`、`code/ecommerce-cart/src/test/java/com/ecommerce/cart/cache/CartCacheManagerTest.java:18,60`，统一 `cart:{userId}` 且 TTL 7 天不变。 |
| 50 | ecommerce-cart | `design-docs/02-系统架构.md` §7 | cart 模块仍存在 `cart`、`cart_item` JPA Entity/Repository 临时明细落库模型。 | 删除或停用 `Cart`、`CartItem`、`CartRepository`、`CartItemRepository` 等持久化模型，临时明细仅保存在缓存。 | Done | 对应 `ECART-004`；已删除 `code/ecommerce-cart/src/main/java/com/ecommerce/cart/entity/Cart.java:1`、`CartItem.java:1`、`CartStatus.java:1`、`code/ecommerce-cart/src/main/java/com/ecommerce/cart/repository/CartRepository.java:1`、`CartItemRepository.java:1`；静态检查无残留引用。 |
| 51 | ecommerce-cart | `README.md` §7.1、§7.2 | cart 使用 `INVALID_QUANTITY`、`CART_FULL` 等未冻结错误码。 | 映射为 `VALIDATION_FAILED` 或按状态冲突映射为 `CONFLICT`，不新增错误码。 | Done | 对应 `ECART-005`；已改 `code/ecommerce-cart/src/main/java/com/ecommerce/cart/service/CartValidationService.java:81,95` 映射为 `VALIDATION_FAILED`，同步 `code/ecommerce-cart/src/test/java/com/ecommerce/cart/service/CartValidationServiceTest.java:139,143,150,154,161,165,181,185`；无 `INVALID_QUANTITY`/`CART_FULL` 残留。 |
| 52 | ecommerce-payment | `design-docs/02-系统架构.md` §3、§6；`README.md` §6.6 | 支付成功回调只更新支付单/订单状态并发布事件，缺少库存扣减。 | payment 接入 `InventoryReservationService#deductAfterPayment`，在支付确认事务内扣减库存，失败回滚。 | Done | 对应 `EPAY-001`；已在 `code/ecommerce-payment/pom.xml:22` 增加 inventory 依赖，`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentCallbackService.java:5,31,35,40,117` 于支付确认事务中先调用 order `markAsPaid` 再调用 `InventoryReservationService#deductAfterPayment`，扣减异常自然抛出回滚；order `markAsPaid` 当前未扣减库存，避免与 order 重复扣减。测试命令 `mvn -f code/pom.xml test -pl ecommerce-payment -am` 已运行，失败于上游/既有测试（common surefire），非本项新增编译错误。 |
| 53 | ecommerce-payment | `design-docs/02-系统架构.md` §4、§6 | payment 未依赖或调用 loyalty `LoyaltyCommandService`。 | 支付成功后的积分发放通过 `LoyaltyCommandService#earnPaymentPoints` 后置处理，异常不回滚支付。 | Done | 对应 `EPAY-002`；已确认并延续现有后置处理链路：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentLoyaltyEventListener.java:14,24,32,36` 监听 `PaymentSucceededEvent` 调用 `LoyaltyCommandService#earnPaymentPoints`，异常仅记录日志不回滚支付。 |
| 54 | ecommerce-payment | `design-docs/02-系统架构.md` §3、§5、§6 | 退款完成后 payment 服务内同步发送通知，未完全通过 `RefundCompletedEvent` 解耦。 | 移除 `RefundService` 直接通知调用，由 common notification 监听 `RefundCompletedEvent` 发送并兜底异常。 | Done | 对应 `EPAY-003`；已从 `code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/RefundService.java:26,33,38,119,122-124` 移除直接通知依赖并改由阶段服务驱动退款完成；与 common notification 监听联动：`code/ecommerce-common/src/main/java/com/ecommerce/common/notification/CoreNotificationEventListener.java:47,62` 监听 `RefundCompletedEvent` 后统一发送通知。 |
| 55 | ecommerce-payment | `design-docs/02-系统架构.md` §6 | 退款流程未按售后单、验收单、退款单分阶段提交。 | 拆分退款生命周期和事务边界，新增/调整售后单、验收单、退款单实体或服务方法。 | Done | 对应 `EPAY-004`；已在 `code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/RefundService.java:85,98,119,122-127` 保留售后审核与仓库验收入口，并新增 `code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/RefundStageService.java:25,41-42,58-59,82-90`，将验收提交与退款完成拆为独立事务阶段，退款事件在完成阶段发布。 |
| 56 | ecommerce-payment | `design-docs/02-系统架构.md` §3、§4 | payment 通过 `OrderDto` 仍依赖 `com.ecommerce.order.entity.OrderStatus`。 | 将订单状态契约移到 query DTO 专用枚举或字符串字段，payment 不再 import order entity 包。 | Done | 对应 `EPAY-005`；已与 order `OrderDto` 状态契约联动，`code/ecommerce-order/src/main/java/com/ecommerce/order/query/OrderDto.java:16,69,73` 使用字符串状态；`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentValidator.java:39-41` 仅基于 DTO 状态字符串判断，payment 模块已无 `com.ecommerce.order.entity.OrderStatus` import。 |
| 57 | ecommerce-payment | `README.md` §7.1、§7.2 | payment 使用 `ORDER_NOT_FOUND`、`PAYMENT_DUPLICATE`、`REFUND_STATUS_INVALID` 等未冻结错误码。 | 映射到 `RESOURCE_NOT_FOUND`、`ORDER_STATUS_CONFLICT`、`CONFLICT`、`VALIDATION_FAILED` 等冻结错误码，保留已冻结支付/退款/发票码。 | Done | 对应 `EPAY-006`；已收敛到冻结错误码：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentValidator.java:41,69` 使用 `ORDER_STATUS_CONFLICT`/`CONFLICT`，`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/RefundService.java:59,93` 与 `RefundStageService.java:47,67,74` 使用 `CONFLICT`/`REFUND_WAITING_WAREHOUSE_ACCEPT`，`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/InvoiceService.java:60` 改为 `CONFLICT`。 |
| 58 | ecommerce-app | `design-docs/02-系统架构.md` §1、§3；`README.md` §6.8 | `EventFailureAdminController` 直接注入 common `FailedEventRecordRepository` 并返回 JPA Entity。 | 在 common 新增失败事件查询服务和 DTO；app Controller 改为注入查询服务并返回 DTO，保持 `/api/v1/admin/events/failures` 契约不变。 | Done | 对应 `EA-001`；common 新增 `code/ecommerce-common/src/main/java/com/ecommerce/common/event/FailedEventRecordItem.java:8` 与 `FailedEventRecordQueryService.java:10` 封装 Repository 查询和 DTO 映射；app 调整 `code/ecommerce-app/src/main/java/com/ecommerce/app/controller/EventFailureAdminController.java:3`、`:19`、`:30` 注入查询服务并返回 DTO，保持 `/api/v1/admin/events/failures` 的 `count`/`records` 顶层字段；联动 ecommerce-common 与 ecommerce-app。 |
