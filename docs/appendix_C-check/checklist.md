# 附录C 修复执行 Checklist

- [x] DONE | ecommerce-inventory | 仓库表名与字段模型不一致：设计为 `warehouses` 且包含 `code`，实现为 `warehouse` 且缺少 `code` | 对应方案：R1 | 实际修改文件：`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/entity/Warehouse.java`、`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/service/WarehouseService.java`、`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/repository/WarehouseRepository.java`、`code/ecommerce-inventory/src/test/java/com/ecommerce/inventory/service/WarehouseServiceTest.java`。 | 已完成修复点：`Warehouse` 表映射为 `warehouses`，新增内部 `code` 列与 getter/setter；仓库创建时生成 `WH-` 前缀内部编码；新增 `findByCode`；`code` 使用 `@JsonIgnore`，不改变冻结 REST 响应字段。 | 剩余问题/原因：无。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-inventory test` 通过，75 tests，0 failures，0 errors。
- [x] DONE | ecommerce-inventory | 库存表预警阈值字段命名不一致：设计为 `warning_threshold`，实现为 `safety_stock` | 对应方案：R2 | 实际修改文件：`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/entity/InventoryStock.java`、`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/service/InventoryService.java`、`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/service/StockWarningService.java`、`code/ecommerce-inventory/src/test/java/com/ecommerce/inventory/repository/InventoryStockRepositoryTest.java`、`code/ecommerce-inventory/src/test/java/com/ecommerce/inventory/service/InventoryServiceTest.java`、`code/ecommerce-inventory/src/test/java/com/ecommerce/inventory/service/InventoryReservationServiceImplTest.java`、`code/ecommerce-inventory/src/test/java/com/ecommerce/inventory/service/StockWarningServiceTest.java`。 | 已完成修复点：`InventoryStock` 落库列改为 `warning_threshold`，实体主字段改为 `warningThreshold`；保留 `getSafetyStock/setSafetyStock` 兼容方法；库存初始化与预警服务改用 `warningThreshold`。 | 剩余问题/原因：对外 `StockWarningResponse.safetyStock` 保留以维持 REST 契约。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-inventory test` 通过，75 tests，0 failures，0 errors。
- [x] DONE | ecommerce-inventory | 库存预占表表名不一致：设计为 `stock_reservations`，实现为 `stock_reservation` | 对应方案：R3 | 实际修改文件：`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/entity/StockReservation.java`。 | 已完成修复点：`StockReservation` 表映射改为 `stock_reservations`；Repository 未使用原生 SQL，无需同步 SQL；预占/释放/扣减字段与枚举语义保持不变。 | 剩余问题/原因：无。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-inventory test` 通过，75 tests，0 failures，0 errors。
- [x] DONE | ecommerce-loyalty | 积分数据模型未实现为设计要求的 `loyalty_points` 表/字段结构 | 对应方案：R4 | 实际修改文件：`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/entity/LoyaltyPoint.java`、`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/repository/LoyaltyPointRepository.java`、`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/service/LoyaltyPointService.java`、`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/service/PointsExpireService.java`。 | 已完成修复点：新增 `loyalty_points(user_id,points,available_points,expire_date,source_type)` 设计表实体与 Repository；积分发放写入设计表，抵扣/冻结扣减 `available_points`，过期任务按 `expire_date DATE` 清零可用积分；查询可优先读取设计表可用积分并保留原账户/流水模型。 | 剩余问题/原因：现有单元测试中直接 new 服务的用例通过兼容构造器降级为原账户模型；完整运行相关模块测试受当前仓库既有跨模块依赖/测试进程问题影响未全部通过。 | 最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-loyalty,ecommerce-promotion,ecommerce-review -am clean compile -DskipTests` 通过；`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-loyalty,ecommerce-review test` 未完成通过，需主代理结合全局测试状态复核。
- [x] DONE | ecommerce-order | `orders` 金额字段精度与附录C要求不一致 | 对应方案：R5 | 实际修改文件：`code/ecommerce-order/src/main/java/com/ecommerce/order/entity/Order.java`。 | 已完成修复点：`item_total`、`shipping_fee`、`packaging_fee`、`discount_amount`、`points_deduction_amount`、`payable_amount`、`paid_amount` 均改为 `precision = 18, scale = 2`，Java 类型与 REST DTO 字段不变。 | 剩余问题/原因：无；订单模块测试因当前仓库已有非本批依赖接口编译问题无法执行测试阶段，本批最小验证改为订单模块编译。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-order -DskipTests compile` 通过；`mvn -f code/pom.xml -pl ecommerce-order,ecommerce-payment test` 在 ecommerce-order 测试前编译失败，失败点为既有 `FreightCalculationService`、`InventoryReservationService`、`ReserveItem` 接口不匹配，非本批修改引入。
- [x] DONE | ecommerce-order | `order_items` 商品快照与金额字段命名/类型与附录C要求不一致 | 对应方案：R6 | 实际修改文件：`code/ecommerce-order/src/main/java/com/ecommerce/order/entity/OrderItem.java`。 | 已完成修复点：落库列改为 `product_name`、`sku_specs CLOB`、`unit_price DECIMAL(18,2)`、`item_amount DECIMAL(18,2)`；保留 `getSkuName/setSkuName`、`getPrice/setPrice`、`getSubtotal/setSubtotal`、`getProductSnapshot/setProductSnapshot` 兼容当前服务与冻结 REST DTO 映射。 | 剩余问题/原因：无；未删除额外 `sku_code` 快照字段。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-order -DskipTests compile` 通过；组合测试命令在订单模块既有跨模块接口不匹配处失败，非本批修改引入。
- [x] DONE | ecommerce-payment | 支付数据模型表名、金额字段、金额精度与状态枚举不一致 | 对应方案：R7 | 实际修改文件：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/entity/PaymentRecord.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/entity/PaymentStatus.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentService.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/RefundStageService.java`、`code/ecommerce-payment/src/test/java/com/ecommerce/payment/**`。 | 已完成修复点：支付表改为 `payments`，核心列显式映射 `payment_no/order_id/amount/method/status/paid_at`；`amount` 为 `DECIMAL(18,2)` 并保留 `getOrderAmount/getPaidAmount` 兼容；初始状态改为 `CREATED`，成功/失败回调为 `SUCCESS/FAILED`，支付状态不再使用 `REFUNDED`。 | 剩余问题/原因：对外 `PayResponse.paidAmount` 字段名保留以维持冻结契约。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-payment test` 通过，53 tests，0 failures，0 errors。
- [x] DONE | ecommerce-payment | 退款数据模型表名、字段、金额精度与状态枚举不一致 | 对应方案：R8 | 实际修改文件：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/entity/RefundRecord.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/entity/RefundStatus.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/RefundService.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/RefundStageService.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/SettlementBatchService.java`、`code/ecommerce-payment/src/test/java/com/ecommerce/payment/**`。 | 已完成修复点：退款表改为 `refunds`，显式映射 `refund_no/order_id/paid_amount/refund_amount/status`；申请退款写入 `paid_amount`；金额精度为 `18,2`；状态流转改为 `APPLIED -> REVIEWED -> ACCEPTED -> REFUNDED` 或 `REJECTED`，结算按 `REFUNDED` 统计。 | 剩余问题/原因：错误码 `REFUND_WAITING_WAREHOUSE_ACCEPT` 未改动；对外 `RefundResponse` 字段名保留。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-payment test` 通过，53 tests，0 failures，0 errors。
- [x] DONE | ecommerce-payment | 发票数据模型表名、字段命名、金额/税率精度与状态枚举不一致 | 对应方案：R9 | 实际修改文件：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/entity/InvoiceRecord.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/entity/InvoiceStatus.java`、`code/ecommerce-payment/src/test/java/com/ecommerce/payment/**`。 | 已完成修复点：发票表改为 `invoices`，核心列显式映射 `invoice_no/order_id/title/amount/tax_rate/tax_amount/status`；金额/税额精度为 `18,2`，税率精度为 `6,4`；状态枚举改为 `ISSUED/VOIDED`，对外 DTO 字段名保持 `invoiceTitle/invoiceAmount`。 | 剩余问题/原因：无；额外 `invoiceType/taxId/remainingInvoiceableAmount` 字段保留为现有业务扩展。 | 最小验证结果：`mvn -f code/pom.xml -pl ecommerce-payment test` 通过，53 tests，0 failures，0 errors。
- [x] DONE | ecommerce-promotion | 优惠券数据模型未按附录C定义实现为 `coupons` 表/实体，字段被拆分并改名 | 对应方案：R10 | 实际修改文件：`code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/entity/Coupon.java`、`code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/repository/CouponRepository.java`、`code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/entity/CouponTemplate.java`、`code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/service/CouponTemplateService.java`、`code/ecommerce-promotion/src/main/java/com/ecommerce/promotion/service/CouponService.java`。 | 已完成修复点：新增 `coupons(coupon_code,type,discount_rate,amount,threshold_amount,valid_from,valid_to)` 设计表实体与 Repository；模板创建和用户领取时同步设计表记录；`DISCOUNT` 写入 `discount_rate`，金额类券写入 `amount`，有效期映射为 `valid_from/valid_to`；`CouponTemplate.threshold_amount` 精度同步为 `18,2`、折扣字段精度同步为 `6,4`。 | 剩余问题/原因：保留 `CouponTemplate/UserCoupon` 模型以维持领取、核销和冻结 REST 契约；促销模块单独测试依赖未纳入 reactor 时仍暴露既有 `ProductQueryService` 测试/依赖不匹配。 | 最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-loyalty,ecommerce-promotion,ecommerce-review -am clean compile -DskipTests` 通过；`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-loyalty,ecommerce-promotion,ecommerce-review test` 在 `ecommerce-promotion` 编译/测试阶段失败，失败点为既有 `CouponValidator` 对 `ProductQueryService.getCategoryIdsBySkuIds` 的跨模块依赖/测试桩不匹配以及旧构造器测试注入，已对本批新增 `CouponRepository` 做兼容构造处理。
- [x] DONE | ecommerce-product | `product_sku.price` 精度与设计 `DECIMAL(18,2)` 不一致 | 对应方案：R11 | 实际修改文件：`code/ecommerce-product/src/main/java/com/ecommerce/product/entity/ProductSku.java`。 | 已完成修复点：将 `ProductSku.price` 从 `@Column(precision = 12, scale = 2)` 改为 `precision = 18, scale = 2`，保持 `BigDecimal` 与 REST DTO/API 不变。 | 剩余问题/原因：无。 | 最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-product,ecommerce-user -am test` 通过，product/user/common 共 70 个测试通过；日志显示 `product_sku` 使用 `price` 与 `specs_json` 写入读取正常。
- [x] DONE | ecommerce-product | `product_sku.specs_json` 字段命名和类型与设计不一致 | 对应方案：R12 | 实际修改文件：`code/ecommerce-product/src/main/java/com/ecommerce/product/entity/ProductSku.java`。 | 已完成修复点：将规格 JSON 落库映射改为 `@Column(name = "specs_json", columnDefinition = "CLOB")`，Java 字段仍为 `specs` 并保留 `getSpecs/setSpecs`，REST 字段名不变。 | 剩余问题/原因：无。 | 最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-product,ecommerce-user -am test` 通过，product/user/common 共 70 个测试通过；Hibernate SQL 显示 `product_sku.specs_json` 插入与查询。
- [x] DONE | ecommerce-user | `users` 表角色字段命名与结构不一致 | 对应方案：R13 | 实际修改文件：`code/ecommerce-user/src/main/java/com/ecommerce/user/entity/User.java`、`code/ecommerce-user/src/main/java/com/ecommerce/user/service/UserAuthService.java`、`code/ecommerce-user/src/main/java/com/ecommerce/user/service/UserQueryServiceImpl.java`。 | 已完成修复点：将用户角色持久化字段改为 `roles VARCHAR(128)`，以逗号分隔字符串表达角色列表；新增 `getRoles/setRoles/getRoleList/setRoleList`，保留 `getRole/setRole(UserRole)` 兼容；登录 JWT 和角色缓存改为使用角色列表，解冻刷新角色列表，用户查询继续映射冻结契约中的单角色字段。 | 剩余问题/原因：无。 | 最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-product,ecommerce-user -am test` 通过，product/user/common 共 70 个测试通过，覆盖注册、登录 JWT、冻结/解冻、鉴权相关用户模块测试。
- [x] DONE | ecommerce-user | `user_addresses` 表默认地址字段命名不一致 | 对应方案：R14 | 实际修改文件：`code/ecommerce-user/src/main/java/com/ecommerce/user/entity/UserAddress.java`。 | 已完成修复点：将 `UserAddress.isDefault` 落库列名从 `is_default` 改为 `default_address`，保持 Java Bean 方法和地址 DTO 映射不变；未发现需同步的原生 SQL。 | 剩余问题/原因：无。 | 最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-product,ecommerce-user -am test` 通过，product/user/common 共 70 个测试通过，覆盖地址创建/查询/默认地址逻辑。
- [x] DONE | ecommerce-review | `reviews.content` 字段类型与附录C不一致 | 对应方案：R15 | 实际修改文件：`code/ecommerce-review/src/main/java/com/ecommerce/review/entity/Review.java`、`code/ecommerce-review/src/main/java/com/ecommerce/review/dto/ReviewCreateRequest.java`。 | 已完成修复点：`Review.content` 从显式 `TEXT` 改为 `@Column(nullable = false, length = 1000)`，由 JPA 映射为 VARCHAR；创建评价请求补充 `@Size(max = 1000)`，超长内容走参数校验；`images`、`reviewerResponse` 等其他 TEXT 扩展字段保持不变。 | 剩余问题/原因：无代码层剩余问题；完整测试需主代理结合当前仓库测试进程状态复核。 | 最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-loyalty,ecommerce-promotion,ecommerce-review -am clean compile -DskipTests` 通过；`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-loyalty,ecommerce-review test` 未完成通过，需主代理结合全局测试状态复核。
