# 附录C 修复执行 Checklist

- [ ] TODO | ecommerce-inventory | 仓库表名与字段模型不一致：设计为 `warehouses` 且包含 `code`，实现为 `warehouse` 且缺少 `code` | 对应方案：R1 | 修改摘要：修改 `Warehouse` 的 `@Table` 为 `warehouses`，新增 `code` 列与 getter/setter，补齐仓库创建/初始化逻辑写入仓库编码，Repository 如需增加 `findByCode`。 | 验证要点：H2 建表包含 `warehouses.code`；仓库创建、入库、库存查询链路通过；REST 契约不变。
- [ ] TODO | ecommerce-inventory | 库存表预警阈值字段命名不一致：设计为 `warning_threshold`，实现为 `safety_stock` | 对应方案：R2 | 修改摘要：将 `InventoryStock.safetyStock` 落库列改为 `warning_threshold`，建议实体字段改为 `warningThreshold` 并保留兼容 getter/setter，更新库存预警和初始化写入读取逻辑。 | 验证要点：`inventory_stock.warning_threshold` 存在；库存可用量计算不变；预警查询仍按阈值工作。
- [ ] TODO | ecommerce-inventory | 库存预占表表名不一致：设计为 `stock_reservations`，实现为 `stock_reservation` | 对应方案：R3 | 修改摘要：修改 `StockReservation` 的 `@Table` 为 `stock_reservations`，同步检查 Repository 原生 SQL 和预占/释放/扣减服务。 | 验证要点：预占、释放、扣减库存流程通过；表名为 `stock_reservations`；状态枚举仍为 `RESERVED/RELEASED/DEDUCTED`。
- [ ] TODO | ecommerce-loyalty | 积分数据模型未实现为设计要求的 `loyalty_points` 表/字段结构 | 对应方案：R4 | 修改摘要：新增或重构 `LoyaltyPoint` 实体与 `LoyaltyPointRepository`，映射 `loyalty_points(user_id, points, available_points, expire_date, source_type)`；积分发放、抵扣、过期与查询逻辑同步写入/读取设计表。 | 验证要点：H2 schema 存在 `loyalty_points.expire_date` DATE；积分查询、积分历史、积分过期、下单抵扣链路通过；账户/流水与设计表余额一致。
- [ ] TODO | ecommerce-order | `orders` 金额字段精度与附录C要求不一致 | 对应方案：R5 | 修改摘要：将 `Order` 中 `item_total`、`shipping_fee`、`packaging_fee`、`discount_amount`、`points_deduction_amount`、`payable_amount`、`paid_amount` 的 `@Column` 精度改为 `precision = 18, scale = 2`。 | 验证要点：订单创建、支付、销售统计通过；金额列 DDL 为 `DECIMAL(18,2)` 或等价；金额计算结果不变。
- [ ] TODO | ecommerce-order | `order_items` 商品快照与金额字段命名/类型与附录C要求不一致 | 对应方案：R6 | 修改摘要：将订单明细落库字段调整为 `product_name`、`sku_specs CLOB`、`unit_price DECIMAL(18,2)`、`item_amount DECIMAL(18,2)`；更新订单创建、查询 DTO、统计/对账读取逻辑，必要时保留旧 getter 兼容。 | 验证要点：订单创建明细写入正确；查询响应字段保持冻结契约；批量订单、销售统计、评价购买校验通过。
- [ ] TODO | ecommerce-payment | 支付数据模型表名、金额字段、金额精度与状态枚举不一致 | 对应方案：R7 | 修改摘要：将 `PaymentRecord` 表名改为 `payments`，显式映射 `payment_no/order_id/amount/method/status/paid_at`；金额精度为 `18,2`；`PaymentStatus` 改为 `CREATED/SUCCESS/FAILED/CLOSED`，服务状态流转同步调整。 | 验证要点：支付单创建返回状态 `CREATED`；支付回调成功为 `SUCCESS`；表名和金额列符合设计；退款不再依赖支付状态 `REFUNDED`。
- [ ] TODO | ecommerce-payment | 退款数据模型表名、字段、金额精度与状态枚举不一致 | 对应方案：R8 | 修改摘要：将 `RefundRecord` 表名改为 `refunds`，新增 `paid_amount DECIMAL(18,2)`，`refund_amount` 精度改为 `18,2`，显式映射 `refund_no/order_id/status`；`RefundStatus` 调整为 `APPLIED/REVIEWED/ACCEPTED/REFUNDED/REJECTED` 并同步服务流转。 | 验证要点：退款申请写入 `paid_amount`；审核、仓库验收、退款完成链路状态正确；业务错误码 `REFUND_WAITING_WAREHOUSE_ACCEPT` 不受破坏。
- [ ] TODO | ecommerce-payment | 发票数据模型表名、字段命名、金额/税率精度与状态枚举不一致 | 对应方案：R9 | 修改摘要：将 `InvoiceRecord` 表名改为 `invoices`，字段映射为 `invoice_no/order_id/title/amount/tax_rate/tax_amount/status`；金额精度 `18,2`，税率精度 `6,4`；`InvoiceStatus.CANCELLED` 改为 `VOIDED`。 | 验证要点：发票开具查询通过；税率可保存四位小数；状态只出现 `ISSUED/VOIDED`；对外 DTO 字段不变。
- [ ] TODO | ecommerce-promotion | 优惠券数据模型未按附录C定义实现为 `coupons` 表/实体，字段被拆分并改名 | 对应方案：R10 | 修改摘要：新增或改造 `Coupon`/Repository 映射 `coupons(coupon_code,type,discount_rate,amount,threshold_amount,valid_from,valid_to)`；创建、领取、计算、核销逻辑从 `CouponTemplate/UserCoupon` 同步或迁移到设计字段；保持 `CouponType` 枚举。 | 验证要点：`coupons` 表字段与精度符合设计；8 折优惠计算仍按 `discount_rate=0.8` 得出正确优惠；优惠券领取和我的优惠券查询通过。
- [ ] TODO | ecommerce-product | `product_sku.price` 精度与设计 `DECIMAL(18,2)` 不一致 | 对应方案：R11 | 修改摘要：将 `ProductSku.price` 的 `@Column(precision = 12, scale = 2)` 改为 `precision = 18, scale = 2`，保持 `BigDecimal` 与 DTO/API 不变。 | 验证要点：商品创建、上架、查询通过；购物车估价和订单价格快照读取正常；DDL 中 `price` 为 `DECIMAL(18,2)` 或等价。
- [ ] TODO | ecommerce-product | `product_sku.specs_json` 字段命名和类型与设计不一致 | 对应方案：R12 | 修改摘要：将规格 JSON 字段映射为 `@Column(name = "specs_json", columnDefinition = "CLOB")`，可将 Java 字段改为 `specsJson` 并保留 `getSpecs/setSpecs` 兼容服务和 DTO。 | 验证要点：商品规格 JSON 可保存查询；订单明细可获取 SKU 规格快照；REST 字段名不变。
- [ ] TODO | ecommerce-user | `users` 表角色字段命名与结构不一致 | 对应方案：R13 | 修改摘要：将用户角色持久化改为 `roles VARCHAR` 列，建议以逗号分隔字符串保存角色列表，保留 `UserRole` 与 `getRole/setRole` 兼容；注册、管理员 seed、登录 JWT、角色缓存和用户查询改为解析 roles。 | 验证要点：普通用户登录 roles 包含 USER；管理员 token 包含 ADMIN；冻结/解冻和鉴权用例通过；`users.roles` 列存在。
- [ ] TODO | ecommerce-user | `user_addresses` 表默认地址字段命名不一致 | 对应方案：R14 | 修改摘要：将 `UserAddress.isDefault` 的列名从 `is_default` 改为 `default_address`，保持 Java Bean 方法和地址 DTO 映射不变，检查 Repository 原生 SQL。 | 验证要点：创建、更新、查询地址通过；默认地址状态正确；表列为 `default_address BOOLEAN`。
- [ ] TODO | ecommerce-review | `reviews.content` 字段类型与附录C不一致 | 对应方案：R15 | 修改摘要：将 `Review.content` 从 `@Column(columnDefinition = "TEXT")` 改为 VARCHAR 映射（如 `@Column(length = 1000)`），补充或确认内容长度校验；其他 TEXT 扩展字段不变。 | 验证要点：评价发布、查询、审核通过；超长内容走校验失败而非数据库异常；`reviews.content` 建表为 VARCHAR。
