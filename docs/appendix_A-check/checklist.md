# 附录A 修复 Checklist

说明：本清单仅覆盖 `docs/appendix_A-check` 已报告的不一致项；每个报告问题保留独立条目。无问题模块：`ecommerce-cart`、`ecommerce-inventory`、`ecommerce-logistics`、`ecommerce-loyalty`、`ecommerce-payment`、`ecommerce-promotion`。

- [x] DONE | ecommerce-app | 安全认证失败响应未按冻结契约返回 401/标准错误结构 | 对应方案：R1 | 实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/security/ApiSecurityErrorWriter.java`、`code/ecommerce-app/src/main/java/com/ecommerce/app/SecurityConfig.java`、`code/ecommerce-app/src/test/java/com/ecommerce/app/config/SecurityConfigTest.java`。 | 已完成修复点：在应用安全链配置统一 `AuthenticationEntryPoint`/`AccessDeniedHandler`；未认证访问受保护 `/api/v1/**` 返回 HTTP 401 与标准 `ApiError(code=UNAUTHORIZED,message,traceId,details={})`；USER 角色访问 ADMIN 接口返回 HTTP 403 与 `code=FORBIDDEN`；同步更新安全配置测试中未认证 USER/ADMIN 接口预期和错误体断言。 | 剩余问题/原因：无；本批不处理 `ecommerce-user` 独立安全链。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-app -am -Dtest=SecurityConfigTest -Dsurefire.failIfNoSpecifiedTests=false clean test` 通过，`SecurityConfigTest` 14 tests，Failures 0，Errors 0。
- [x] DONE | ecommerce-common | 通用异常处理未按冻结错误码契约返回部分业务错误 HTTP 状态 | 对应方案：R2 | 实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/exception/GlobalExceptionHandler.java`、`code/ecommerce-common/src/test/java/com/ecommerce/common/exception/GlobalExceptionHandlerTest.java`。 | 已完成修复点：`GlobalExceptionHandler.handleBusiness` 改为按冻结错误码映射 HTTP 状态；`USER_NOT_ACTIVE`、`USER_FROZEN`、`REVIEW_PURCHASE_REQUIRED` 返回 403；`ORDER_STATUS_CONFLICT`、`REFUND_WAITING_WAREHOUSE_ACCEPT` 返回 409；其他普通 `BusinessException` 维持 400；`ApiError` 结构不变。 | 剩余问题/原因：无。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-common,ecommerce-order -am -Dtest=GlobalExceptionHandlerTest,OrderValidatorTest,OrderServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过；`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-common,ecommerce-review -am -Dtest=GlobalExceptionHandlerTest,ReviewControllerTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过。
- [x] DONE | ecommerce-order | 冻结或未激活用户下单错误码要求为 403，但当前订单模块会按通用业务异常返回 400 | 对应方案：R2 | 实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/exception/GlobalExceptionHandler.java`、`code/ecommerce-common/src/test/java/com/ecommerce/common/exception/GlobalExceptionHandlerTest.java`。 | 已完成修复点：不修改 `OrderPreconditionChecker` 已使用的 `USER_FROZEN`、`USER_NOT_ACTIVE` 错误码，通过公共 `BusinessException` 状态映射使订单创建前置校验返回 HTTP 403，body code 保持对应冻结错误码。 | 剩余问题/原因：无。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-common,ecommerce-order -am -Dtest=GlobalExceptionHandlerTest,OrderValidatorTest,OrderServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，覆盖公共映射及订单服务相关回归。
- [x] DONE | ecommerce-order | 订单状态冲突错误码要求为 409，但当前订单模块状态冲突路径会返回 400 | 对应方案：R2 | 实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/exception/GlobalExceptionHandler.java`、`code/ecommerce-common/src/test/java/com/ecommerce/common/exception/GlobalExceptionHandlerTest.java`。 | 已完成修复点：保留 `OrderCancelService`、`OrderStateMachine` 抛出的 `ORDER_STATUS_CONFLICT`，通过公共 `BusinessException` 状态映射将其返回 HTTP 409，body code 保持 `ORDER_STATUS_CONFLICT`。 | 剩余问题/原因：无。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-common,ecommerce-order -am -Dtest=GlobalExceptionHandlerTest,OrderValidatorTest,OrderServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，`GlobalExceptionHandlerTest` 已断言 `ORDER_STATUS_CONFLICT`/`REFUND_WAITING_WAREHOUSE_ACCEPT` 为 409。
- [x] DONE | ecommerce-order | 订单金额非法错误码要求为 `ORDER_INVALID_AMOUNT`，但当前金额校验会暴露非冻结契约错误码 | 对应方案：R3 | 实际修改文件：`code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderValidator.java`、`code/ecommerce-order/src/test/java/com/ecommerce/order/service/OrderValidatorTest.java`。 | 已完成修复点：在 `OrderValidator.validateAmount` 中捕获 `MoneyValidationUtil.validatePayableAmount` 抛出的内部 `OrderValidationException`，重新抛出 code 为 `ORDER_INVALID_AMOUNT` 的订单校验异常；HTTP 仍由现有 `handleOrderValidation` 返回 400；未修改订单 URL、Method、DTO 字段、成功 201 响应。 | 剩余问题/原因：无；公共 `MoneyValidationUtil` 内部错误码保持不变，避免影响非订单上下文。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-common,ecommerce-order -am -Dtest=GlobalExceptionHandlerTest,OrderValidatorTest,OrderServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，`OrderValidatorTest` 已覆盖 null/zero/negative 金额均暴露 `ORDER_INVALID_AMOUNT`，正常金额通过，`OrderServiceTest` 正常创建和风控回归通过。
- [x] DONE | ecommerce-product | 管理商品接口的认证/鉴权失败未按冻结错误响应结构返回 | 对应方案：R1 | 实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/security/ApiSecurityErrorWriter.java`、`code/ecommerce-app/src/main/java/com/ecommerce/app/SecurityConfig.java`、`code/ecommerce-app/src/test/java/com/ecommerce/app/config/SecurityConfigTest.java`。 | 已完成修复点：未修改商品管理接口路径或 `@PreAuthorize("hasRole('ADMIN')")` 语义；通过 `ecommerce-app` 安全链统一覆盖 `/api/v1/admin/products/**` 认证/鉴权失败响应；补充 `POST /api/v1/admin/products/spu` 未认证 401/`UNAUTHORIZED`、USER 角色 403/`FORBIDDEN` 的标准错误体测试。 | 剩余问题/原因：无。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-app -am -Dtest=SecurityConfigTest -Dsurefire.failIfNoSpecifiedTests=false clean test` 通过，`SecurityConfigTest` 14 tests，Failures 0，Errors 0。
- [x] DONE | ecommerce-review | 评价发布相关 403 错误码通过 `BusinessException` 返回为 400 | 对应方案：R2 | 实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/exception/GlobalExceptionHandler.java`、`code/ecommerce-common/src/test/java/com/ecommerce/common/exception/GlobalExceptionHandlerTest.java`、`code/ecommerce-review/src/test/java/com/ecommerce/review/controller/ReviewControllerTest.java`。 | 已完成修复点：不修改 `ReviewService` 中已抛出的 `USER_NOT_ACTIVE`、`USER_FROZEN`、`REVIEW_PURCHASE_REQUIRED` 错误码，通过公共 `BusinessException` 状态映射返回 HTTP 403；评价发布购买门槛失败测试预期同步改为 403，body `code=REVIEW_PURCHASE_REQUIRED`。 | 剩余问题/原因：无。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-common,ecommerce-review -am -Dtest=GlobalExceptionHandlerTest,ReviewControllerTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，`ReviewControllerTest` 7 tests，Failures 0，Errors 0。
- [x] DONE | ecommerce-user | Spring Security 拦截的未认证/无权限错误未按冻结错误响应契约返回 | 对应方案：R1 | 实际修改文件：`code/ecommerce-user/src/main/java/com/ecommerce/user/config/SecurityConfig.java`、`code/ecommerce-user/src/test/java/com/ecommerce/user/controller/AddressControllerTest.java`、`code/ecommerce-user/src/test/java/com/ecommerce/user/controller/UserControllerTest.java`、`code/ecommerce-user/src/test/java/com/ecommerce/user/controller/AdminUserControllerTest.java`。 | 已完成修复点：在 `ecommerce-user` 独立安全链配置与 app 一致的 `AuthenticationEntryPoint`/`AccessDeniedHandler`，复用公共 `ApiSecurityErrorWriter`；未认证访问 `/api/v1/users/me`、`/api/v1/users/addresses`、`/api/v1/admin/users/{userId}/freeze|unfreeze` 返回 HTTP 401 与标准 `ApiError(code=UNAUTHORIZED,message,traceId,details={})`；USER 角色访问 ADMIN 接口返回 HTTP 403 与 `code=FORBIDDEN`；同步更新 `AddressControllerTest`、`UserControllerTest`、`AdminUserControllerTest` 的状态码与标准错误体断言。 | 剩余问题/原因：无。 | 该批最小验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-user -am -Dtest=AddressControllerTest,UserControllerTest,AdminUserControllerTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，20 tests，Failures 0，Errors 0。