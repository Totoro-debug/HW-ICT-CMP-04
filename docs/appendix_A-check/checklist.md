# 附录A 修复 Checklist

说明：本清单仅覆盖 `docs/appendix_A-check` 已报告的不一致项；每个报告问题保留独立条目。无问题模块：`ecommerce-cart`、`ecommerce-inventory`、`ecommerce-logistics`、`ecommerce-loyalty`、`ecommerce-payment`、`ecommerce-promotion`。

- [ ] TODO | ecommerce-app | 安全认证失败响应未按冻结契约返回 401/标准错误结构 | 对应方案：R1 | 修改摘要：在 `code/ecommerce-app/src/main/java/com/ecommerce/app/SecurityConfig.java` 配置 `exceptionHandling`，为未认证配置标准 `UNAUTHORIZED`/401 `ApiError`，为拒绝访问配置 `FORBIDDEN`/403 `ApiError`；可复用 `ecommerce-common` 公共安全错误写出组件；同步调整 `SecurityConfigTest` 中未认证访问 USER 接口的 403 预期。 | 验证要点：未带 Bearer Token 访问 `/api/v1/**` 受保护 USER 接口返回 401；body 包含 `code=UNAUTHORIZED`、`message`、非空 `traceId`、对象类型 `details`；USER token 访问 ADMIN 接口返回 403 和 `code=FORBIDDEN`。
- [ ] TODO | ecommerce-common | 通用异常处理未按冻结错误码契约返回部分业务错误 HTTP 状态 | 对应方案：R2 | 修改摘要：在 `code/ecommerce-common/src/main/java/com/ecommerce/common/exception/GlobalExceptionHandler.java` 的 `handleBusiness` 中按错误码映射 HTTP 状态：`USER_NOT_ACTIVE`、`USER_FROZEN`、`REVIEW_PURCHASE_REQUIRED` -> 403；`ORDER_STATUS_CONFLICT`、`REFUND_WAITING_WAREHOUSE_ACCEPT` -> 409；其他普通 `BusinessException` 维持 400；更新 `GlobalExceptionHandlerTest`。 | 验证要点：直接构造上述 `BusinessException` 时响应体仍为 `code/message/traceId/details`，HTTP 状态分别为 403/409；普通业务错误码仍为 400。
- [ ] TODO | ecommerce-order | 冻结或未激活用户下单错误码要求为 403，但当前订单模块会按通用业务异常返回 400 | 对应方案：R2 | 修改摘要：不修改 `OrderPreconditionChecker` 已使用的 `USER_FROZEN`、`USER_NOT_ACTIVE` 错误码，通过公共 `GlobalExceptionHandler.handleBusiness` 映射使订单创建前置校验返回 403。 | 验证要点：冻结用户或未激活用户调用 `POST /api/v1/orders/create` 返回 HTTP 403；body `code` 分别为 `USER_FROZEN`/`USER_NOT_ACTIVE`，错误结构字段完整。
- [ ] TODO | ecommerce-order | 订单状态冲突错误码要求为 409，但当前订单模块状态冲突路径会返回 400 | 对应方案：R2 | 修改摘要：保留 `OrderCancelService`、`OrderStateMachine` 抛出的 `ORDER_STATUS_CONFLICT`，通过公共 `GlobalExceptionHandler.handleBusiness` 映射为 HTTP 409；必要时更新订单取消、取消审核、状态机相关测试预期。 | 验证要点：对不可取消状态订单调用取消/取消审核，或触发非法状态转换时返回 HTTP 409；body `code=ORDER_STATUS_CONFLICT`，结构字段完整。
- [ ] TODO | ecommerce-order | 订单金额非法错误码要求为 `ORDER_INVALID_AMOUNT`，但当前金额校验会暴露非冻结契约错误码 | 对应方案：R3 | 修改摘要：在 `code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderValidator.java` 的 `validateAmount` 中将 `MoneyValidationUtil.validatePayableAmount` 产生的内部金额异常转换为对外 `ORDER_INVALID_AMOUNT`；保持 HTTP 400 与订单接口 Request/Response 不变。 | 验证要点：订单 itemTotal 或 payableAmount 为空、为 0、低于 0.01 或计算后非法时，接口返回 HTTP 400 且 body `code=ORDER_INVALID_AMOUNT`；不得再暴露 `PAYABLE_AMOUNT_TOO_LOW`/`DISCOUNT_AMOUNT_INVALID`；正常创建订单仍为 201。
- [ ] TODO | ecommerce-product | 管理商品接口的认证/鉴权失败未按冻结错误响应结构返回 | 对应方案：R1 | 修改摘要：商品管理接口本身路径和 `@PreAuthorize("hasRole('ADMIN')")` 不变；通过 `ecommerce-app` 安全链的统一 `AuthenticationEntryPoint`/`AccessDeniedHandler` 覆盖 `/api/v1/admin/products/**` 的 401/403 标准错误响应。 | 验证要点：未认证访问 `POST /api/v1/admin/products/spu` 返回 401 和 `code=UNAUTHORIZED`；USER 角色访问返回 403 和 `code=FORBIDDEN`；body 均包含 `message/traceId/details`。
- [ ] TODO | ecommerce-review | 评价发布相关 403 错误码通过 `BusinessException` 返回为 400 | 对应方案：R2 | 修改摘要：不修改 `ReviewService` 中已抛出的 `USER_NOT_ACTIVE`、`USER_FROZEN`、`REVIEW_PURCHASE_REQUIRED` 错误码，通过公共 `GlobalExceptionHandler.handleBusiness` 映射为 HTTP 403；更新评价发布相关测试状态码断言。 | 验证要点：未激活/冻结用户或未购买商品调用 `POST /api/v1/reviews` 返回 HTTP 403；body `code` 为对应业务错误码，错误结构字段完整。
- [ ] TODO | ecommerce-user | Spring Security 拦截的未认证/无权限错误未按冻结错误响应契约返回 | 对应方案：R1 | 修改摘要：在 `code/ecommerce-user/src/main/java/com/ecommerce/user/config/SecurityConfig.java` 配置与 app 一致的 `exceptionHandling`；确认 `JwtAuthFilter` 缺失 Header、格式错误 Header、JWT 校验失败后由统一 entry point 返回 401；同步调整 `AddressControllerTest`、`UserControllerTest` 未认证访问的 403 预期并补充标准错误体断言。 | 验证要点：未认证访问 `/api/v1/users/me`、`/api/v1/users/addresses` 返回 401 和 `code=UNAUTHORIZED`；USER 角色访问 `/api/v1/admin/users/{userId}/freeze` 返回 403 和 `code=FORBIDDEN`；错误 JSON 固定包含 `code/message/traceId/details`。