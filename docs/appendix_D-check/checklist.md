# 附录D 修复实施 Checklist

- [x] DONE | ecommerce-common | 公共事件基类未支持 `eventType`、`aggregateId`、`traceId` 通用字段（`docs/appendix_D-check/ecommerce-common-check.md:22-26`） | 对应方案：R1 | 当前状态：已修复。实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/event/AbstractDomainEvent.java`。已完成修复点：新增 `eventType`、`aggregateId`、`traceId` 字段和 getter；保留 `AbstractDomainEvent(Object source)` 兼容构造器；新增附录D事件可调用的完整通用字段构造器，默认 `eventType=getClass().getSimpleName()`、`traceId=eventId`。剩余问题/原因：无；其他模块失败记录对通用字段的消费由后续对应批次继续补齐。验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-common test` 通过；`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-review -am -DskipTests compile`、`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-payment -am -DskipTests compile` 通过。

- [x] DONE | ecommerce-common | `OrderPaidEvent` 载荷缺少 `items`（`docs/appendix_D-check/ecommerce-common-check.md:28-32`） | 对应方案：R2 | 当前状态：已完成 common 事件结构与本批直接发布点最小修复。实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/event/OrderPaidEvent.java`、`code/ecommerce-common/src/main/java/com/ecommerce/common/event/OrderPaidEventItem.java`、`code/ecommerce-order/src/main/java/com/ecommerce/order/query/OrderDto.java`、`code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderQueryServiceImpl.java`、`code/ecommerce-order/src/main/java/com/ecommerce/order/event/OrderPaidEvent.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentService.java`。已完成修复点：新增订单商品明细事件 DTO；`OrderPaidEvent` 增加 `items` 字段、getter、完整构造器并保留旧构造器；payment 发布的兼容 `OrderPaidEvent` 从 `OrderDto` 明细组装 `items`；order 包别名构造器透传 `items`。剩余问题/原因：order-service 正式发布方 `OrderPaymentEventHandler`、logistics/loyalty 监听消费更新不在本批范围，需后续批次处理。验证结果：common 单测通过；review/payment 相关依赖编译通过。

- [x] DONE | ecommerce-common | common notification 对 `PaymentSucceededEvent` 未使用 `paidAt`（`docs/appendix_D-check/ecommerce-common-check.md:34-38`） | 对应方案：R3 | 当前状态：已修复本批 common notification 消费部分。实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/notification/CoreNotificationEventListener.java`。已完成修复点：`buildPaymentSucceededNotification` 读取 `paidAt` 并写入通知变量；失败记录仍使用完整事件序列化。剩余问题/原因：inventory/logistics/loyalty/order 对 `PaymentSucceededEvent` 的监听消费由后续批次处理。验证结果：`ecommerce-common` 单测通过；`ecommerce-payment -am -DskipTests compile` 通过。

- [x] DONE | ecommerce-common | `ReviewApprovedEvent` 载荷缺少 `orderId`、`productId`（`docs/appendix_D-check/ecommerce-common-check.md:40-44`） | 对应方案：R8 | 当前状态：已修复 common 事件结构。实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/event/ReviewApprovedEvent.java`。已完成修复点：新增 `orderId`、`productId` 字段、getter 和完整构造器；保留旧构造器兼容；设置 `eventType="ReviewApprovedEvent"`、`aggregateId=reviewId`、默认 `traceId`。剩余问题/原因：loyalty 监听方 payload 增强不在本批范围。验证结果：common 单测通过；review 相关依赖编译通过。

- [x] DONE | ecommerce-inventory | 库存监听 `OrderPaidEvent`，未按附录D监听 `PaymentSucceededEvent`（`docs/appendix_D-check/ecommerce-inventory-check.md:21-34`） | 对应方案：R4 | 当前状态：已完成 inventory 侧最小修复并修正依赖方向。实际修改文件：`code/ecommerce-inventory/src/main/java/com/ecommerce/inventory/service/InventoryPaymentSucceededEventListener.java`、`code/ecommerce-inventory/src/test/java/com/ecommerce/inventory/service/InventoryPaymentSucceededEventListenerTest.java`。已完成修复点：将库存扣减监听语义从 `OrderPaidEvent` 调整为 `PaymentSucceededEvent`；未在 inventory 引入 payment 模块编译依赖，监听方法采用 `@EventListener(condition = "#root.event.class.name == 'com.ecommerce.payment.event.PaymentSucceededEvent'")` + `Object` + 反射读取载荷；监听内部捕获扣减异常并持久化失败记录，不向发布方传播；失败记录 `eventType` 改为 `InventoryPaymentSucceededEventListener:PaymentSucceededEvent`；payload 写入 `eventId`、`eventType`、`occurredAt`、`aggregateId`、`traceId` 以及 `paymentNo`、`orderId`、`paidAmount`、`paidAt`；replay 继续按 `orderId` 执行 `deductAfterPayment`。剩余问题/原因：强类型监听需等待模块依赖重构后统一处理。验证结果：`mvn -f D:/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-inventory -am -Dtest=InventoryPaymentSucceededEventListenerTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，2 tests，0 failures，0 errors。

- [x] DONE | ecommerce-logistics | logistics-service 未实现 `PaymentSucceededEvent` 监听入口（`docs/appendix_D-check/ecommerce-logistics-check.md:20-24`） | 对应方案：R5 | 当前状态：已修复。实际修改文件：`code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/event/OrderPaidShipmentListener.java`、`code/ecommerce-logistics/src/test/java/com/ecommerce/logistics/event/OrderPaidShipmentListenerTest.java`。已完成修复点：新增 `onPaymentSucceeded(PaymentSucceededEvent)` 监听入口，按 `orderId` 调用 `LogisticsCommandService.createShipmentForPaidOrder`；失败记录 payload 写入 `eventId`、`eventType`、`occurredAt`、`aggregateId`、`traceId`、`paymentNo`、`orderId`、`paidAmount`、`paidAt`；监听异常在方法内捕获并保存失败记录，不向支付主流程传播；replay 兼容旧 `orderId` 字符串和新 JSON payload。剩余问题/原因：无。验证结果：`mvn -f D:/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-logistics -am -Dtest=OrderPaidShipmentListenerTest,ShipmentServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过；其中 `OrderPaidShipmentListenerTest` 6 tests，0 failures，0 errors；`ShipmentServiceTest` 18 tests，0 failures，0 errors。

- [x] DONE | ecommerce-logistics | logistics 消费的 `OrderPaidEvent` 缺少 `items`（`docs/appendix_D-check/ecommerce-logistics-check.md:26-30`） | 对应方案：R2 | 当前状态：已修复 logistics 监听消费部分。实际修改文件：`code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/event/OrderPaidShipmentListener.java`、`code/ecommerce-logistics/src/test/java/com/ecommerce/logistics/event/OrderPaidShipmentListenerTest.java`。已完成修复点：`onOrderPaid` 继续只按 `orderId` 创建发货单，保持物流创建契约和幂等能力；日志记录 `items` 数量；失败记录 payload 保留 `items` 明细及通用字段，兼容 R2 后的完整 `OrderPaidEvent`。剩余问题/原因：无。验证结果：`mvn -f D:/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-logistics -am -Dtest=OrderPaidShipmentListenerTest,ShipmentServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过；其中 `OrderPaidShipmentListenerTest` 6 tests，0 failures，0 errors；`ShipmentServiceTest` 18 tests，0 failures，0 errors。

- [x] DONE | ecommerce-logistics | `ShipmentDeliveredEvent` 通用字段不齐全（`docs/appendix_D-check/ecommerce-logistics-check.md:32-36`） | 对应方案：R5 | 当前状态：已修复。实际修改文件：`code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/event/ShipmentDeliveredEvent.java`、`code/ecommerce-logistics/src/main/java/com/ecommerce/logistics/service/ShipmentService.java`。已完成修复点：`ShipmentDeliveredEvent` 构造器调用公共事件基类完整字段构造器，设置 `eventType="ShipmentDeliveredEvent"`、`aggregateId=orderId`、默认 `traceId`；事件载荷聚焦 `orderId`、`shipmentId`、`deliveredAt`，移除 `userId` 载荷；`ShipmentService.updateStatus` 在 DELIVERED 发布点按新构造器传入 `orderId`、`shipmentId`、`deliveredAt`。剩余问题/原因：无。验证结果：`mvn -f D:/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-logistics -am -Dtest=OrderPaidShipmentListenerTest,ShipmentServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过；其中 `OrderPaidShipmentListenerTest` 6 tests，0 failures，0 errors；`ShipmentServiceTest` 18 tests，0 failures，0 errors。

- [x] DONE | ecommerce-loyalty | 本地事件通用字段实现不完整，loyalty 无法读取完整字段（`docs/appendix_D-check/ecommerce-loyalty-check.md:20-24`） | 对应方案：R1 | 当前状态：已修复 loyalty 消费侧失败记录。实际修改文件：`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/OrderPaidEventListener.java`、`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/PaymentSucceededEventListener.java`、`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ShipmentDeliveredEventListener.java`、`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ReviewApprovedEventListener.java` 及对应事件监听测试。已完成修复点：四类 loyalty 事件失败记录 payload 均补齐 `eventId`、`eventType`、`occurredAt`、`aggregateId`、`traceId`，并设置 `lastError`、`status=PENDING`。剩余问题/原因：无；通用字段来源依赖前序 R1 公共事件基类修复。验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-loyalty -am -Dtest=OrderPaidEventListenerTest,PaymentSucceededEventListenerTest,ShipmentDeliveredEventListenerTest,ReviewApprovedEventListenerTest -Dsurefire.failIfNoSpecifiedTests=false test` 通过，8 tests，0 failures，0 errors。

- [x] DONE | ecommerce-loyalty | `OrderPaidEvent` 缺少 `items` 且 loyalty 使用非契约 `paymentNo`（`docs/appendix_D-check/ecommerce-loyalty-check.md:26-30`） | 对应方案：R7 | 当前状态：已修复 loyalty 监听消费部分。实际修改文件：`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/OrderPaidEventListener.java`、`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/event/OrderPaidEventListenerTest.java`。已完成修复点：`OrderPaidEventListener` 日志读取 `items` 数量；失败记录 payload 写入 `items` 明细和通用字段；积分幂等 `bizId` 改用 `eventId`，不再依赖 `paymentNo`。剩余问题/原因：无。验证结果：loyalty 事件监听测试命令通过，覆盖支付积分发放与失败记录。

- [x] DONE | ecommerce-loyalty | `PaymentSucceededEvent` 缺少 `paidAt` 且 loyalty 依赖非契约 `userId`（`docs/appendix_D-check/ecommerce-loyalty-check.md:32-36`） | 对应方案：R7 | 当前状态：已修复 loyalty 监听消费部分。实际修改文件：`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/PaymentSucceededEventListener.java`、`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/event/PaymentSucceededEventListenerTest.java`。已完成修复点：监听日志和失败 payload 读取/记录 `paymentNo`、`orderId`、`paidAmount`、`paidAt` 及通用字段；移除对事件载荷 `userId` 的日志和失败记录依赖。剩余问题/原因：当前 loyalty 侧该监听只作为附录D入口记录，不发放积分；支付积分仍由 payment 侧适配器调用 `LoyaltyCommandService`。验证结果：loyalty 事件监听测试命令通过，断言失败记录包含 `paidAt` 且不包含 `userId`。

- [x] DONE | ecommerce-loyalty | `ShipmentDeliveredEvent` loyalty 侧依赖非契约 `userId`（`docs/appendix_D-check/ecommerce-loyalty-check.md:38-42`） | 对应方案：R7 | 当前状态：已修复。实际修改文件：`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ShipmentDeliveredEventListener.java`、`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/event/ShipmentDeliveredEventListenerTest.java`。已完成修复点：日志和失败 payload 仅使用 `orderId`、`shipmentId`、`deliveredAt` 与通用字段，删除 `getUserId` 反射依赖。剩余问题/原因：无；当前业务规则不额外发放签收积分。验证结果：loyalty 事件监听测试命令通过，断言失败记录包含 `deliveredAt` 且不包含 `userId`。

- [x] DONE | ecommerce-loyalty | `ReviewApprovedEvent` 缺少 `orderId`、`productId`（`docs/appendix_D-check/ecommerce-loyalty-check.md:44-48`） | 对应方案：R7 | 当前状态：已修复 loyalty 监听消费部分。实际修改文件：`code/ecommerce-loyalty/src/main/java/com/ecommerce/loyalty/event/ReviewApprovedEventListener.java`、`code/ecommerce-loyalty/src/test/java/com/ecommerce/loyalty/event/ReviewApprovedEventListenerTest.java`。已完成修复点：日志和失败 payload 增加 `orderId`、`productId` 及通用字段；评价积分发放继续以 `reviewId` 幂等。剩余问题/原因：无；事件结构依赖前序 R8 公共事件与 review 发布点修复。验证结果：loyalty 事件监听测试命令通过，覆盖评价积分发放、配置积分和失败记录。

- [x] DONE | ecommerce-order | `OrderPaidEvent` 事件契约字段不完整（`docs/appendix_D-check/ecommerce-order-check.md:20-24`） | 对应方案：R2 | 当前状态：订单侧已完成最小修复。实际修改文件：`code/ecommerce-common/src/main/java/com/ecommerce/common/event/AbstractDomainEvent.java`、`code/ecommerce-common/src/main/java/com/ecommerce/common/event/OrderPaidEvent.java`、`code/ecommerce-common/src/main/java/com/ecommerce/common/event/OrderPaidEventItem.java`、`code/ecommerce-order/src/main/java/com/ecommerce/order/event/OrderPaidEvent.java`、`code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderPaymentEventHandler.java`，并保留 payment 侧兼容发布点使用 `OrderDto.items`。已完成修复点：order 包别名事件透传 `items`；`OrderPaymentEventHandler` 从 `OrderItemRepository` 组装 `items` 并发布完整 `OrderPaidEvent`；公共基类/事件提供通用字段与 `items` getter。剩余问题/原因：payment 侧仍会兼容发布 `OrderPaidEvent`，需主代理在后续批次统一评估是否去重；`productId` 当前订单明细无独立字段时按现有 DTO 兼容来源填充。验证结果：尝试执行 `mvn -f D:\Desktop\work\HW-ICT-CMP-04\code\pom.xml -pl ecommerce-order -am test` 与 `... -DskipTests compile`，本机 `mvn` 命令不可用（`Get-Command mvn` 失败），未能完成 Maven 验证。

- [x] DONE | ecommerce-order | order-service 未按 `PaymentSucceededEvent` 作为监听方消费事件（`docs/appendix_D-check/ecommerce-order-check.md:26-30`） | 对应方案：R6 | 当前状态：订单侧已完成最小兼容监听。实际修改文件：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/event/PaymentSucceededEvent.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentService.java`、`code/ecommerce-order/src/main/java/com/ecommerce/order/listener/OrderEventListener.java`、`code/ecommerce-order/src/main/java/com/ecommerce/order/service/OrderPaymentEventHandler.java`。已完成修复点：`PaymentSucceededEvent` 暴露 `paidAt` 与通用字段；订单监听器通过事件类名监听并消费 `paymentNo/orderId/paidAmount/paidAt`；支付成功处理支持事件时间并保持已 PAID 幂等返回；监听异常写入失败记录而不向发布方传播。剩余问题/原因：为避免 order 模块 POM 引入 payment/logistics 循环依赖，订单侧采用 `Object` + 事件类名 + 反射读取载荷的兼容监听；强类型监听可由主代理在模块依赖重构后统一处理。验证结果：尝试执行订单模块测试/编译，但本机缺少 `mvn` 命令，未能完成 Maven 验证。

- [x] DONE | ecommerce-order | order-service 未按 `ShipmentDeliveredEvent` 作为监听方消费事件（`docs/appendix_D-check/ecommerce-order-check.md:32-36`） | 对应方案：R6 | 当前状态：订单侧已完成最小兼容监听。实际修改文件：`code/ecommerce-order/src/main/java/com/ecommerce/order/listener/OrderEventListener.java`。已完成修复点：订单监听器通过事件类名监听 `ShipmentDeliveredEvent`，消费 `orderId/shipmentId/deliveredAt`；订单侧将非完成订单推进到 `DELIVERED` 并记录 `SHIPMENT_DELIVERED` 订单事件；监听异常持久化失败记录，避免回滚发布方主流程。剩余问题/原因：为避免 order 模块直接依赖 logistics 形成 Maven 环，采用反射兼容监听；物流侧事件通用字段和强类型发布仍归 R5/后续批次处理。验证结果：尝试执行订单模块测试/编译，但本机缺少 `mvn` 命令，未能完成 Maven 验证。

- [x] DONE | ecommerce-payment | `PaymentSucceededEvent` 缺少 `eventType`、`aggregateId`、`traceId` 通用字段（`docs/appendix_D-check/ecommerce-payment-check.md:21-25`） | 对应方案：R1 | 当前状态：已修复。实际修改文件：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/event/PaymentSucceededEvent.java`。已完成修复点：构造器调用 R1 完整通用字段构造器，设置 `eventType="PaymentSucceededEvent"`、`aggregateId=orderId`，`traceId` 由基类默认补齐。剩余问题/原因：payment 事件失败记录完整字段消费由后续监听/失败记录批次继续处理。验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-payment -am -DskipTests compile` 通过。

- [x] DONE | ecommerce-payment | `PaymentSucceededEvent` 载荷缺少 `paidAt`（`docs/appendix_D-check/ecommerce-payment-check.md:27-31`） | 对应方案：R3 | 当前状态：已修复。实际修改文件：`code/ecommerce-payment/src/main/java/com/ecommerce/payment/event/PaymentSucceededEvent.java`、`code/ecommerce-payment/src/main/java/com/ecommerce/payment/service/PaymentService.java`。已完成修复点：事件增加 `paidAt` 字段和 getter；保留旧构造器并默认使用当前时间；`PaymentService.confirmPayment` 优先使用支付记录 `paidAt`，为空时设置并保存，再发布携带 `paidAt` 的 `PaymentSucceededEvent`。剩余问题/原因：order/inventory/logistics/loyalty 对 `paidAt` 的消费由后续批次处理。验证结果：`ecommerce-payment -am -DskipTests compile` 通过。

- [x] DONE | ecommerce-review | `ReviewApprovedEvent` 发布对象缺少通用字段及 `orderId`、`productId`（`docs/appendix_D-check/ecommerce-review-check.md:21-31`） | 对应方案：R8 | 当前状态：已修复发布方。实际修改文件：`code/ecommerce-review/src/main/java/com/ecommerce/review/service/ReviewModerationService.java`、`code/ecommerce-common/src/main/java/com/ecommerce/common/event/ReviewApprovedEvent.java`。已完成修复点：`ReviewModerationService.approve` 从 `Review` 实体读取 `orderId`、`productId`，发布完整 `ReviewApprovedEvent`；事件对象包含 `reviewId`、`userId`、`orderId`、`productId` 与完整通用字段。剩余问题/原因：loyalty 监听方字段记录不在本批范围。验证结果：`mvn -f /d/Desktop/work/HW-ICT-CMP-04/code/pom.xml -pl ecommerce-review -am -DskipTests compile` 通过。
